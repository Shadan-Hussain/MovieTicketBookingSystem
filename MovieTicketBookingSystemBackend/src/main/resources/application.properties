spring.application.name=MovieTicketBookingSystem
server.port=${SERVER_PORT:8081}

# Database
# Prefer Spring Boot standard env vars (SPRING_DATASOURCE_URL/USERNAME/PASSWORD).
# DB_* vars are supported for convenience in local dev.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:movie_ticket_booking_system_db}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DB_USERNAME:postgres}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:}}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Stripe (optional for base app boot; required only when calling payment/webhook endpoints)
stripe.secretKey=${STRIPE_SECRET_KEY:}
stripe.webhookSecret=${STRIPE_WEBHOOK_SECRET:}
# Redirect base: set APP_BASE_URL in prod; locally defaults to http://localhost:server.port
stripe.successUrl=${APP_BASE_URL:http://localhost:${server.port}}/redirect/success?session_id={CHECKOUT_SESSION_ID}
stripe.cancelUrl=${APP_BASE_URL:http://localhost:${server.port}}/redirect/cancel

# Redis (optional for base app boot; required only when calling lock/payment endpoints)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
# Enable TLS for Redis when talking to Valkey serverless (set SPRING_DATA_REDIS_SSL_ENABLED=true in env on AWS)
spring.data.redis.ssl.enabled=${SPRING_DATA_REDIS_SSL_ENABLED:false}
